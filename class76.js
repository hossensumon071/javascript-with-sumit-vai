// Javascript Functions Apply()
    /*
      .with the apply() method, you can write a method that can be used on deffrent objects
      .The apply() method similer to the call() method

      The defernces between call() and apply() method
         .the call() method takes arguments separately
         .the apply() method takes arguments as an array
    */